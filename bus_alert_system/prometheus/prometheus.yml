global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Prometheus itself
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Kafka JMX Exporter (if you add JMX exporter later)
  # - job_name: 'kafka'
  #   static_configs:
  #     - targets: ['kafka:9101']

  # Spring Boot Actuator (if you add Actuator to your application)
  # - job_name: 'bus-reminder-app'
  #   static_configs:
  #     - targets: ['host.docker.internal:8080']
  #   metrics_path: '/actuator/prometheus'

